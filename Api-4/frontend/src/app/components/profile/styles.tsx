import styled from 'styled-components';

export const Title = styled.p`
    color: #090909;
    font-weight: bold;
`;

export const ProfileField = styled.img`
    width: 60px;
    height: 60px;
    border-radius: 50%;
    object-fit: cover;
    border: 2px solid #fff;
    cursor: pointer;
`;